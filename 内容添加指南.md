# 如何添加新内容 & 视频

## 一、添加新对话

打开 `chunks.json`，在最后一个 `}` 后面加逗号，然后粘贴模板：

```json
{
    "id": "c031",
    "chinese": "你的中文句子",
    "category": ["日常"],
    "frequency": "high",
    "expressions": [
      {
        "text": "美国人最常说的版本",
        "usageProbability": 80,
        "context": "使用场景和注意事项（中文写，方便学习者理解）",
        "tone": "casual",
        "video": null
      },
      {
        "text": "另一种说法",
        "usageProbability": 20,
        "context": "这个版本的说明",
        "tone": "neutral",
        "video": null
      }
    ]
  }
```

要点：
- `id` 每次 +1（c031, c032...）
- `tone`：`casual`（随意）/ `neutral`（中性）/ `formal`（正式）/ `slang`（俚语）
- `usageProbability` 加起来 ≈ 100%
- `category` 可选：日常、社交、工作、吃饭、健康、情感、购物、出行
- `video` 先写 `null`，找到视频再补

---

## 二、添加 YouTube 视频（YouGlish 大法）

### 最推荐方式：YouGlish

1. 打开 **[youglish.com](https://youglish.com)**
2. 输入英文表达，比如 `Are you down for`
3. 它会自动播放 YouTube 上美国人说这句话的片段
4. 找到喜欢的片段后，注意两个东西：
   - **YouTube 视频 ID**：点右下角 "Watch on YouTube"，地址栏 `v=` 后面的那串，比如 `dQw4w9WgXcQ`
   - **起止时间**：这句话从第几秒开始？到下一句结束是第几秒？

### 怎么填到 chunks.json

把 `"video": null` 改成：

```json
"video": {
    "youtubeId": "dQw4w9WgXcQ",
    "startTime": 15,
    "endTime": 28,
    "title": "街头采访-朋友约饭"
}
```

- `youtubeId`：YouTube 视频 ID（不是完整链接，只要 v= 后面的那串）
- `startTime`：这句话从第几秒开始
- `endTime`：到下一句话结束是第几秒
- `title`：随便写，帮自己记住是什么视频

网站会自动只播放 startTime → endTime 这一段，不需要你剪辑。

---

## 三、改完怎么更新

终端里跑：

```bash
cd ~/chunk-app && git add -A && git commit -m "更新内容" && git push
```

等 1-2 分钟刷新网站就看到了。

---

## 四、如果你不想自己写 JSON

把你的中文句子 + 场景发给我，我帮你生成：
- 地道美式表达（按概率排序）
- 场景说明
- B站/YouTube 视频搜索建议

然后直接更新到网站。
