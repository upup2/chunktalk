# 如何添加新内容 & 视频

## 一、添加新对话（复制粘贴改一下就行）

打开 `chunks.json`，找到最后一个 `}` 后面，加一个逗号，然后粘贴下面的模板：

```json
{
    "id": "c026",
    "chinese": "你写的这里的中文句子",
    "category": ["日常"],
    "frequency": "high",
    "expressions": [
      {
        "text": "美国人最常说的版本",
        "usageProbability": 80,
        "context": "这段话在什么场景用、有什么要注意的",
        "tone": "casual",
        "video": null
      },
      {
        "text": "另外一种说法",
        "usageProbability": 20,
        "context": "这个版本的使用场景说明",
        "tone": "neutral",
        "video": null
      }
    ]
  }
```

要点：
- `id` 每次加 1（c026, c027, c028...）
- `tone` 有四种：`casual`（随意）、`neutral`（中性）、`formal`（正式）、`slang`（俚语）
- `usageProbability` 加起来 100%
- `category` 可选：日常、社交、工作、吃饭、健康、情感、购物、出行
- `video` 可以先写 `null`，等找到视频再补

---

## 二、添加 B 站视频

### 怎么找 B 站上的英语视频

1. 打开 [bilibili.com](https://www.bilibili.com)
2. 搜你想找的英文表达，比如搜 `What have you been up to`
3. 加上关键词更好搜：`美剧片段`、`英语口语`、`vlog`、`英语场景`
4. 找一个美国人（或英语母语者）说出这句话的片段
5. 记下两个东西：
   - **BV 号**：在视频页面的地址栏，像 `BV1xx411x7xx`
   - **时间戳**：这句话在视频的几分几秒

### 怎么填到 chunks.json 里

把 `"video": null` 替换成：

```json
"video": {
    "bvid": "BV1xx411x7xx",
    "startTime": 15,
    "endTime": 28,
    "title": "《老友记》朋友见面场景"
}
```

- `bvid`：B站视频的 BV 号
- `startTime`：这句话从第几秒开始
- `endTime`：到第几秒结束
- `title`：随便写，方便自己记住是什么视频

---

## 三、改完怎么更新到网站

在 VSCode 底部的终端里依次输入：

```bash
cd ~/chunk-app
git add chunks.json
git commit -m "添加新内容"
git push
```

推送后 GitHub Pages 会自动更新（等 1-2 分钟刷新网址就看到新内容了）。

---

## 四、去哪里找灵感

- **小红书**搜"英语口语"找中国人常问的表达
- **B站**搜"英语口语 日常对话"有大量素材
- **美剧台词**：《老友记》《摩登家庭》《生活大爆炸》是口语宝库
- **YouTube 搬运到 B站**的内容：搜"英语母语者"、"美国人说话方式"
- 日常生活里你自己想说但说不出口的话，记下来，去查美国人会怎么说
